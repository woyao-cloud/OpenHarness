# 🐍 贪吃蛇游戏 — 完整规划书

> 目标：构建一个功能完整、体验流畅、视觉精美的贪吃蛇游戏。
> 技术栈：纯前端 HTML + CSS + JavaScript (Canvas)，零依赖，单文件即可运行。

---

## 一、需求总览

| 功能 | 优先级 | 描述 |
|------|--------|------|
| ✅ 核心玩法 | P0 | 键盘控制蛇移动，吃食物增长，撞墙/撞己游戏结束 |
| ✅ 计分系统 | P0 | 实时显示当前分数、最高分（localStorage 持久化） |
| ✅ 难度选择 | P1 | 简单/普通/困难，影响基础速度与加速曲线 |
| ✅ 自动加速 | P1 | 每吃 N 个食物速度提升一级，有上限 |
| ✅ 穿墙模式 | P1 | 开关式，蛇从一边穿到另一边 |
| ✅ 暂停/继续 | P1 | Space 键切换 |
| ✅ 音效系统 | P2 | Web Audio API，吃食物/加速/结束/胜利各有音效 |
| ✅ 移动端支持 | P2 | 触摸滑动操控 |
| ✅ 胜利判定 | P2 | 蛇占满所有格子时胜利 |
| ✅ 视觉特效 | P2 | 渐变色蛇身、发光蛇头、闪烁食物、圆角网格 |

---

## 二、系统架构

```
┌─────────────────────────────────────────┐
│               HTML 结构                   │
│  ┌───────────────────────────────────┐  │
│  │  标题 / 计分板 / Canvas / 控制栏   │  │
│  └───────────────────────────────────┘  │
├─────────────────────────────────────────┤
│              CSS 样式层                  │
│  深色主题 / 霓虹风格 / 响应式布局        │
├─────────────────────────────────────────┤
│           JavaScript 逻辑层              │
│  ┌──────────┐ ┌──────────┐ ┌────────┐  │
│  │ 游戏状态  │ │ 渲染引擎  │ │ 音效系统│  │
│  │ 管理(SM)  │ │ (Canvas) │ │ (Audio)│  │
│  ├──────────┤ ├──────────┤ ├────────┤  │
│  │ 碰撞检测  │ │ 食物生成  │ │ 输入处理│  │
│  │          │ │          │ │ 键盘+触│  │
│  └──────────┘ └──────────┘ └────────┘  │
└─────────────────────────────────────────┘
```

### 核心模块划分

#### 1. 状态管理模块 (`Game State`)
- 蛇的数据结构：坐标数组 `[{x,y}, ...]`
- 方向：`direction` / `nextDirection`（防止 180° 掉头）
- 食物坐标
- 分数、游戏状态（进行中/暂停/结束/胜利）
- 难度参数、速度等级

#### 2. 渲染引擎 (`Renderer`)
- 基于 Canvas 2D
- 网格背景绘制
- 蛇头（发光 + 眼睛）、蛇身（渐变色）
- 食物（径向渐变红球）
- 结束/胜利/暂停遮罩层
- `roundRect` polyfill（兼容旧浏览器）

#### 3. 游戏循环 (`Game Loop`)
- `setInterval` 驱动（比 `requestAnimationFrame` 更适合固定 tick 游戏）
- 每次 tick: `update()` → `draw()`
- 速度动态调整：停止旧 interval，启动新 interval

#### 4. 输入处理 (`Input Handler`)
- 键盘：方向键 + WASD
- 触摸：滑动方向识别
- 防 180° 掉头逻辑

#### 5. 音效系统 (`Audio System`)
- Web Audio API（懒初始化）
- 合成器音效：吃食物、加速、游戏结束、胜利
- 无需外部音频文件

#### 6. 碰撞检测 (`Collision Detection`)
- 墙壁碰撞（普通模式 vs 穿墙模式）
- 自身碰撞
- 食物拾取检测

#### 7. 食物生成 (`Food Spawner`)
- 随机坐标，避开蛇身
- 若格子几乎占满 → 触发胜利

---

## 三、数据流

```
用户输入 (键盘/触摸)
       │
       ▼
  handleKey() ──→ 更新 nextDirection
       │
       ▼
  update() (每 tick)
       │
       ├── 1. 应用方向 direction = nextDirection
       ├── 2. 计算新蛇头坐标
       ├── 3. 检测是否吃到食物
       │      ├── 是 → 蛇头插入，不删除尾，分数+1，生成新食物，播放音效
       │      └── 否 → 蛇头插入，删除尾
       ├── 4. 碰撞检测
       │      ├── 撞墙 → 穿墙模式? 穿越 : gameOver
       │      ├── 撞己 → gameOver
       │      └── 占满 → win
       └── 5. 调用 draw()
       │
       ▼
  draw() ──→ Canvas 渲染
```

---

## 四、难点与解决方案

| 难点 | 解决方案 |
|------|----------|
| **蛇不能 180° 掉头** | 存储 `direction` 和 `nextDirection`，`update()` 时才应用；校验新旧方向不相反 |
| **穿墙模式** | 碰撞墙壁时取模：`(x + GRID_SIZE) % GRID_SIZE` |
| **蛇身渐变色** | 根据段索引 `idx / snake.length` 计算 RGB 渐变 |
| **食物不生成在蛇身上** | 随机试错，最多 1000 次；若全满则胜利 |
| **移动端触摸** | 记录 `touchstart` 坐标，`touchend` 时计算滑动方向（忽略 <20px 的误触） |
| **音效兼容** | Web Audio API 需要用户交互后才能激活；懒初始化，try-catch 静默降级 |
| **响应式布局** | 小屏设备容器 padding 自适应，Canvas 保持 400x400 固定分辨率 |

---

## 五、文件结构

```text
snake.html          ← 单文件完整游戏（当前不完整，需修复）
docs/
  snake-game-plan.md ← 本文档
```

> 设计上保持单文件，便于分享和部署。所有资源（样式、逻辑、音效）内嵌。

---

## 六、现有 `snake.html` 问题分析

| 问题 | 严重性 | 修复方案 |
|------|--------|----------|
| ❌ `TICK_INTERVAL` 未定义 → `start()` 崩溃 | 🔴 致命 | 改用 `currentInterval` 全局变量驱动 |
| ❌ 穿墙模式未实现 | 🟡 功能缺失 | `update()` 中取模处理坐标 |
| ❌ 音效函数定义了但从未调用 | 🟡 功能缺失 | 在 `update()` 中合适位置调用 |
| ❌ 速度加速逻辑未实现 | 🟡 功能缺失 | 每吃 N 个食物缩短 interval |
| ❌ 难度切换 Select 无事件监听 | 🟡 功能缺失 | 添加 `change` 事件 |
| ❌ 穿墙按钮无事件监听 | 🟡 功能缺失 | 添加 `click` 事件 toggle |
| ⚠️ 蛇头穿墙时视觉平滑处理 | 🟢 体验优化 | 穿墙后坐标取模 |

---

## 七、实施步骤

### Step 1 — 修复核心 Bug
- 定义 `TICK_INTERVAL` 或改用 `currentInterval`
- 确保 `start()` 正常启动游戏循环

### Step 2 — 实现缺失功能
- 穿墙模式（取模运算 + 开关）
- 自动加速（每 5 个食物缩短 interval）
- 难度切换监听
- 音效调用

### Step 3 — 完善视觉与体验
- 蛇头穿墙平滑处理
- 游戏结束 / 胜利后的键盘禁用
- 移动端适配测试

### Step 4 — 测试与优化
- 边界条件测试（撞墙、撞己、占满胜利）
- 难度切换时的速度重置
- 最高分 localStorage 读写验证

---

## 八、未来可扩展方向

- 🎮 双人模式（分屏 / 同屏）
- 🧩 障碍物 + 地图关卡
- 🏆 在线排行榜（后端 API）
- 🌐 多语言 i18n
- 🎨 自定义皮肤/主题
- 📱 PWA 打包为 App

---

> **规划版本**: v1.0  
> **最后更新**: 2025-04-27  
> **状态**: ✅ 规划完成，进入实施阶段
